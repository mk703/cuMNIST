调度器：维持两个queue，一个是可运行操作queue，另一个是暂时不可运行的queue，在可执行queue执行过一个操作之后将其放回不可执行queue，全部放回后再查看不可执行queue有哪些操作可以执行再放到可执行queue
执行器：处理op与其绑定的地址
stream_signal：
设计一个中间tensor类，保存依赖*op、张量地址和event，设置一个函数检查只有依赖op全执行完了才删除自己，开关是否是常数
用算子类和tensor类画一幅dag，互相保存需要的地址